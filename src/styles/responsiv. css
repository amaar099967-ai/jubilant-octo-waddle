/* التجاوب لجميع الأجهزة */

/* شاشات كبيرة جداً (أكبر من 1400px) */
@media (min-width: 1400px) {
  .container {
    max-width: 1320px;
  }
  
  .sidebar {
    width: 300px;
  }
  
  .main-content {
    margin-right: 300px;
  }
}

/* شاشات متوسطة (992px إلى 1199px) */
@media (max-width: 1199px) and (min-width: 992px) {
  .sidebar {
    width: 220px;
  }
  
  .main-content {
    margin-right: 220px;
  }
  
  .card-deck .card {
    margin-bottom: 15px;
  }
}

/* شاشات صغيرة (768px إلى 991px) */
@media (max-width: 991px) and (min-width: 768px) {
  .sidebar {
    width: 200px;
  }
  
  .main-content {
    margin-right: 200px;
    padding: 15px;
  }
  
  .table-responsive {
    font-size: 14px;
  }
  
  .btn {
    padding: 6px 12px;
    font-size: 14px;
  }
}

/* شاشات الأجهزة اللوحية (576px إلى 767px) */
@media (max-width: 767px) and (min-width: 576px) {
  .sidebar {
    position: fixed;
    z-index: 1000;
    width: 250px;
    transform: translateX(100%);
    transition: transform 0.3s ease;
  }
  
  .sidebar.show {
    transform: translateX(0);
  }
  
  .main-content {
    margin-right: 0;
    padding: 10px;
  }
  
  .navbar-toggler {
    display: block;
  }
  
  .table th,
  .table td {
    padding: 8px;
  }
  
  .card-header h5 {
    font-size: 16px;
  }
  
  .btn-lg {
    padding: 8px 16px;
    font-size: 14px;
  }
}

/* شاشات الهواتف (أقل من 576px) */
@media (max-width: 575px) {
  body {
    font-size: 14px;
  }
  
  .sidebar {
    width: 100%;
    height: auto;
    position: fixed;
    bottom: 0;
    top: auto;
    z-index: 1000;
    transform: translateY(100%);
  }
  
  .sidebar.show {
    transform: translateY(0);
  }
  
  .main-content {
    padding-bottom: 60px; /* مساحة للشريط السفلي */
  }
  
  .sidebar-header {
    padding: 10px;
  }
  
  .sidebar-menu {
    display: flex;
    overflow-x: auto;
    padding: 10px 0;
  }
  
  .sidebar-menu .nav-link {
    white-space: nowrap;
    padding: 8px 15px;
    margin: 0 5px;
    border-radius: 20px;
  }
  
  .navbar-brand {
    font-size: 16px;
  }
  
  .card {
    margin-bottom: 10px;
  }
  
  .card-body {
    padding: 12px;
  }
  
  .table-responsive {
    margin: 0 -10px;
  }
  
  .table {
    font-size: 12px;
  }
  
  .table th,
  .table td {
    padding: 6px;
  }
  
  .form-control,
  .form-select {
    padding: 8px 12px;
    font-size: 14px;
  }
  
  .btn {
    padding: 6px 12px;
    font-size: 13px;
  }
  
  .modal-dialog {
    margin: 10px;
  }
  
  .modal-content {
    border-radius: 8px;
  }
  
  /* تنسيقات صفحة تسجيل الدخول للهواتف */
  .login-container {
    margin: 10px;
    padding: 20px;
  }
  
  /* تنسيقات الجدول المحاسبي للهواتف */
  .project-table-container {
    overflow-x: auto;
  }
  
  .financial-box .row > div {
    margin-bottom: 10px;
  }
  
  /* إخفاء بعض العناصر في الهواتف */
  .d-none-mobile {
    display: none !important;
  }
  
  /* عرض العناصر بشكل عمودي */
  .stack-on-mobile {
    flex-direction: column;
  }
  
  .stack-on-mobile > * {
    width: 100%;
    margin-bottom: 10px;
  }
  
  /* تصغير النصوص في الهواتف */
  h1 { font-size: 24px; }
  h2 { font-size: 20px; }
  h3 { font-size: 18px; }
  h4 { font-size: 16px; }
  h5 { font-size: 15px; }
  h6 { font-size: 14px; }
  
  /* تنسيقات القوائم المنسدلة للهواتف */
  .dropdown-menu {
    position: fixed !important;
    top: auto !important;
    bottom: 0 !important;
    left: 0 !important;
    right: 0 !important;
    width: 100% !important;
    max-height: 70vh;
    overflow-y: auto;
    border-radius: 15px 15px 0 0;
  }
  
  /* تنسيقات أدوات التنسيق للهواتف */
  .formatting-tools .row {
    flex-direction: column;
  }
  
  .formatting-tools .col-md-3,
  .formatting-tools .col-md-9 {
    width: 100%;
  }
}

/* التوجيه الأفقي للهواتف */
@media (max-height: 500px) and (orientation: landscape) {
  .sidebar {
    width: 200px;
    height: 100vh;
    overflow-y: auto;
  }
  
  .main-content {
    margin-right: 200px;
  }
  
  .login-page {
    min-height: auto;
    padding: 20px 0;
  }
  
  .login-container {
    max-height: 90vh;
    overflow-y: auto;
  }
}

/* دعم للشاشات عالية الدقة (ريتينا) */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
  .btn,
  .form-control,
  .card {
    border-width: 0.5px;
  }
}

/* دعم للوضع الداكن */
@media (prefers-color-scheme: dark) {
  body.dark-mode {
    background-color: #121212;
    color: #ffffff;
  }
  
  body.dark-mode .card {
    background-color: #1e1e1e;
    color: #ffffff;
  }
  
  body.dark-mode .table {
    background-color: #1e1e1e;
    color: #ffffff;
  }
  
  body.dark-mode .form-control {
    background-color: #2d2d2d;
    color: #ffffff;
    border-color: #444;
  }
}

/* دعم للأجهزة التي تدعم اللمس */
@media (hover: none) and (pointer: coarse) {
  .btn {
    padding: 12px 24px; /* مساحة أكبر لللمس */
  }
  
  .form-control,
  .form-select {
    min-height: 44px; /* الحد الأدنى لللمس */
  }
  
  .dropdown-item {
    padding: 12px 20px;
  }
  
  .table th,
  .table td {
    padding: 12px;
  }
}

/* دعم للشاشات الصغيرة جداً */
@media (max-width: 320px) {
  body {
    font-size: 13px;
  }
  
  .container {
    padding-left: 5px;
    padding-right: 5px;
  }
  
  .btn {
    padding: 5px 10px;
    font-size: 12px;
  }
  
  .modal-dialog {
    margin: 5px;
  }
}
